# figure2

Cactus whole-genome alignment summary (Figure 2).

- `Figure2_cactus_alignment.ipynb` — notebook that produces the figure.
- `cactus_aligmnent.pdf` — rendered output.

Inputs are read from `../data/cactus_aligmnent.tsv`.
